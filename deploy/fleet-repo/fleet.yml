images:
  picolet: "ghcr.io/schjan/picolet:v0.1.0"

ports:
  # Documents host port for firewall rules and Prometheus scrape config.
  # Network=host means no actual port publishing is needed.
  picolet_metrics: 9417
