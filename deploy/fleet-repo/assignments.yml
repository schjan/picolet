base: {}

roles:
  # Production: rootful Podman, system-level systemd
  worker:
    services:
      - picolet-system
  controller:
    services:
      - picolet-system

  # Dev/test: rootless Podman, user-level systemd
  test:
    services:
      - picolet

features: {}
