base: {}

pi_types:
  # Production: rootful Podman, system-level systemd
  worker:
    services:
      - picolet
  controller:
    services:
      - picolet

  # Dev/test: rootless Podman, user-level systemd
  test:
    containers:
      - quadlets/rootless/picolet.container.tmpl

features: {}
