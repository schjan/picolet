  - name: instance_alerts
    rules:
      - alert: InstanceDown
        expr: up == 0
        for: 5m
        annotations:
          summary: "{{ $labels.instance }} of job {{ $labels.job }} is down"
