[Container]
Image={{ index .Images "simple" }}
ContainerName=picolet-e2e-test
Network=internal.network
Secret=e2e_secret
Label=hostname={{ .Host.Hostname }}
Label=external={{ .Host.ExternalHostname }}
Exec=sleep infinity

[Install]
WantedBy=default.target
